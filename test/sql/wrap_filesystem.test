# name: test/sql/wrap_filesystem.test
# description: test observefs extension wrapping duckdb-compatible filesystems
# group: [sql]

require observefs

statement error
SELECT observefs_wrap_filesystem('unregistered_filesystem');
----
Invalid Input Error: Filesystem unregistered_filesystem hasn't been registered yet!

statement ok
SELECT observefs_wrap_filesystem('observefs_fake_filesystem');
